import { Card } from "@/components/common/Card";

export default function ServerPage() {
  return (
    <Card>
      <h2 className="text-xl font-semibold text-zinc-50">Server shell</h2>
      <p className="mt-2 text-sm text-zinc-400">Table and payment workflows are reserved for later phases.</p>
    </Card>
  );
}
