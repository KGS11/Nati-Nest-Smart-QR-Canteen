import { Card } from "@/components/common/Card";

export default function AdminPage() {
  return (
    <Card>
      <h2 className="text-xl font-semibold text-zinc-50">Admin shell</h2>
      <p className="mt-2 text-sm text-zinc-400">Dashboard business modules arrive in a later phase.</p>
    </Card>
  );
}
